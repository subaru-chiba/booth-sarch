services:
  - type: web
    name: booth-sarch
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 run.py
    envVars:
      - key: DB_PATH
        value: /data/booth_trends.db
    disk:
      name: booth-data
      mountPath: /data
      sizeGB: 1
